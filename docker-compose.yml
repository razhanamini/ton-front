services:
  frontend:
    image: ton-frontend:latest
    container_name: ton-frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    networks:
      - ton-network

networks:
  ton-network:
    external: true
    name: ton-network